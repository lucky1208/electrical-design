# 变更记录

## 1.1.0 — 架构分层：布局 / 布线 / 渲染分离

### 背景

1.0.13 的结构性问题：`design-model.js` 每次生成都产出完整网表（45 设备 / 111 端口 /
84 回路），但 `draw-pile.js` 对 `R.design` 的引用次数是 **0** —— 渲染器绕开工程模型，
用手写坐标把同一份拓扑重画一遍。于是校验器（几何自检是全局的）比生成器（手摆坐标）强，
216 组参数中有 18 组因单个未处理交叉被闸门阻断，且全部是**国标 4 枪**这一常见商用配置。

### 第 0 步 止血

- 交叉半圆改为**统一后处理** `SYM.resolveCrossings`，复用绘图闸门自身的检测口径，
  「生产端满足校验端」从碰运气变成构造性保证；结果与绘制顺序无关。
- 删除三处「边画边查局部注册表」的手工跳线逻辑（`fanAC` / `segVs,segHs` / `segV2,segH2`），
  以及互不知情的两套注册表 `RV,RH` 与 `RV2,RH2` —— 正是国标 4 枪阻断的成因。
- L 形走线不再输出 `<polyline>`（闸门几何自检只解析 `<line>`，polyline 是盲区）。
- 走线带 `data-w` 标记：后处理只打断走线，不打断设备符号内部线。
- **结果：216/216 通过（原 198/216）。**

### 第 1 步 布局层 `placement.js`

- 坐标从代码变成数据：分区、基准线、串联链路、设备 bbox、端口锚点、走线通道、障碍矩形。
- 交流/直流串联链路的 x 由序列推导，不再手写（推导值与原字面量完全吻合）。
- 重构过程以 72 组像素基线校验，**输出逐字节不变**。
- 渲染器中的坐标字面量从 60+ 降到 7 个。

### 第 2 步 布线层 `router.js`

- 通道模型替代像素级 A*：原理图是稀疏图，通道 + 轨道更快、更规整、更符合制图习惯。
- 轨道分配用**区间图着色**：只有 x 区间重叠的走线才必须分轨，因此 LIB-R19 的
  「平行信号线错开 ≥6px」是构造性保证，不再靠人工摆 `i*10` 阶梯。
- 障碍避让：撞设备的轨道自动跳过；端点落在自身设备框内不误判为撞设备。
- 联锁信号改为向上短接到 A1 底边，不再绕最左侧走廊折返。
- 布不通时**显式在图上告警并计数**，绝不静默叠线。

### 第 3 步 渲染层瘦身 + 图↔网表一致性闸门

- 走线带 `data-circuit` / `data-conductor` / `data-role`；半圆弧继承同样的身份
  （弧是导线的一部分）。
- 新增三条阻断式闸门，补上此前**完全缺失**的一类检查：
  - `G050-NET-COVERAGE` 网表回路必须画出，或在规则包中登记为已知缺口并写明理由；
  - `G051-NET-ORPHAN` 图上每根走线必须可归属，且不得出现网表里没有的回路；
  - `G052-NET-CONTINUITY` 同一导线的线段必须首尾相连，不得出现断开的孤立段。
- 已知缺口清单 `UNDRAWN_CIRCUITS` 放在**规则包**而非渲染器 —— 渲染器不能自己给自己发合格证。

### 测试

新增 `tests/`：引擎回归、几何不变量、布线层、图↔网表一致性、规则包、图面体检，
以及 216 组参数矩阵扫描 `npm run test:matrix`（建议进 CI，通过率必须 100%）。

### 已知待决

- `C.dc` 与 `C.saf` 目前是同一个红 `#dc2626`，图例把「充电直流回路」和「安全器件」
  列为两条却同色，看图人和按颜色分类的检查都无法区分。建议为安全回路单列一色
  （需同步更新 `EVSE-COLOR-SCHEME` 白名单与 LIB-R14）。
- 回路覆盖率目前 39%~43%，其余已在 `UNDRAWN_CIRCUITS` 登记。继续提升覆盖率
  即可让「图 = 模型」的机器校验更严密。
