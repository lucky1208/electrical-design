# EVSE Schematic Design 2.5 — CAD 选择与精确编辑验收

验收日期：2026-09-03

版本：2.5.0

编辑内核：`EVSE_SCHEMATIC_EDITOR 2.1.0`

局部路由内核：`EVSE_SCHEMATIC_EDIT_ROUTER 1.0.0`

## 1. 验收结果

在 `D:\charging_pile\gpt\evse-schematic-design-1.0.14` 执行：

```powershell
npm run build
npm test
```

结果：

- 全量测试：`198 / 198` 通过；失败 0；跳过 0；总耗时 518416 ms；
- 216 高密度参数组合：`216 / 216` 通过；矩阵耗时 513773 ms；
- 五接口 × 四桩型：`20 / 20` 通过；
- 编辑器、局部路由、源码同步与 Web 契约定向测试：`37 / 37` 通过；
- Web bundle：由 27 个 `engine/*.js` 模块确定性生成，源/镜像字节一致；
- 最终缓存键：bundle `sha256-a3f9516f84db0cb1872fdd0cbea8ffa544c788eb3d35ac3c4a7f0491c8a1e34b`，app `sha256-e2a5554d50ec3befee117c7bcfc78a70aafe087737acfabeecbd43bc0454f3aa`。

公共 GitHub 发布树包含额外的 11 项 Vercel API 代理测试。对发布树执行同一套 `npm test` 的最终结果为：总计 `209` 项，`208` 通过、失败 `0`、`1` 项按设计跳过；跳过项仅检查未随公共仓库分发的客户原始 SVG 文件哈希。发布树的 216 参数矩阵仍为 `216 / 216` 通过，矩阵耗时 513718 ms，全量耗时 519113 ms。

## 2. 新增能力

| 能力 | 验收结论 |
|---|---|
| 选择集与主对象 | PASS；器件、回路、图注及合法子对象使用稳定选择键，单选兼容旧接口 |
| CAD 双向框选 | PASS；左→右要求对象完全包含，右→左选择与框相交对象 |
| 组合选择 | PASS；Shift 增选、Ctrl/Cmd 切换、Ctrl/Cmd+A 选择可见器件与图注 |
| 成组拖动 | PASS；选中器件与图注作为一个 `MOVE_OBJECTS` 事务提交 |
| 动态网格 | PASS；1、2.5、5、10、20 单位可选，显示和吸附可独立开关 |
| 键盘微调 | PASS；方向键按网格步长移动，Shift 为五倍步长 |
| 对齐与分布 | PASS；六向对齐、水平/垂直等距分布保持精确几何约束 |
| 双端差量 | PASS；同一回路两端器件位移不同时仍分别保持精确 PIN 坐标 |
| 原子回滚 | PASS；任一对象碰撞、越界、无合法路由或 coverage 失败时整组不落盘 |
| 单步撤销 | PASS；每个组命令只占一条历史记录，一次撤销恢复完整组和原哈希 |
| 历史上限 | PASS；普通命令、reset 与 redo 均服从配置的 history limit |
| 会话不可变性 | PASS；Drawing IR 与嵌套 metadata 隔离并深冻结，外部不能绕过 revision 改图 |
| 电气身份门禁 | PASS；器件、端口、网络、回路、物理引用、端点和别名追踪不得被几何命令改变 |
| 实时模型 coverage | PASS；已有 coverage 的图纸必须绑定实时 EDEM，启动和每次提交都重新审计 |

## 3. 真实浏览器验收

使用本地 Web 默认参数生成 120 kW 国标双枪图，完成以下实际鼠标与键盘操作：

- Shift 点击 `QS1` 与 `QF1` 得到两个高亮对象，主对象以独立颜色显示；Ctrl 点击可切换单个对象；
- 成组拖动两个器件提交一个 `MOVE_OBJECTS` 命令，自动重布 17 条回路，图纸审计保持 `CHECKED`；
- `Ctrl+Z` 一次恢复原始 Drawing IR 哈希 `fnv1a32:1938a898`；
- 网格切换为 10 后用方向键成组微调，自动重布 16 条回路；一次撤销恢复原哈希；
- 顶部对齐提交一个 `ALIGN_DEVICES` 命令并重布 8 条回路；一次撤销恢复原哈希；
- 左→右窗口框选只命中完全包含的 `QF1`；右→左窄框命中所有相交器件与回路；
- 在拥挤交流区域尝试不安全等距分布时，命令以 `DEVICE_BODY_COLLISION` 拒绝，哈希不变，审计仍为 `CHECKED`。

## 4. 安全不变量

- Web 编辑的是 Drawing IR 对象，不是扁平 SVG 像素；SVG、DXF 和 JSON 始终消费同一当前 Drawing IR；
- 导线可以选择和检查，但不允许通过成组平移脱离端子；现有导线几何只允许受约束的中间线段编辑；
- 任何几何命令都不能创建、删除或猜测器件、PIN、网络、回路、端点或能量域；
- 每个事务都重新构建 Drawing IR、比较电气身份、执行几何审计并校验实时模型 coverage；
- 路由器没有穿器件 fallback；找不到合法正交路径只能拒绝并恢复命令前状态；
- 选择变化和拖动预览不增加 revision、不写入 journal、不改变几何哈希。

## 5. 尚未完成

2.5 不宣称已经完成多页层次图、跨页端口、经过 EDEM 审批的新建/删除器件与网络、复制粘贴、旋转、镜像、对象锁定、位号批量管理、成熟 EDA 的实时 lane-by-lane 递归推挤，或短路/保护选择性、EMC、温升、绝缘配合与最终标准合规证明。

本验收只证明已有受控端子模型上的选择、几何编辑、局部重布和事务回滚满足当前自动测试与真实浏览器场景。输出仍是方案级概念草图，必须由电气专业人员复核和签发。
